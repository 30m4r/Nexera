# config.py
# Shared network configuration parameters for the Multi-Client Socket System

HOST = '127.0.0.1'  # Standard loopback interface address (localhost)
PORT = 8080        # Port to listen on (non-privileged ports are > 1023)
BUFFER_SIZE = 1024 # Standard buffer size for sending/receiving data
ENCODING = 'utf-8' # Standard encoding for string transmission
